
version: 2

models:
    - name: dim_date
      description: "A standard calendar table"
      columns:
          - name: date_day
            description: "The date"
            tests:
                - unique
                - not_null
    - name: dim_time
      description: "The time of the day"
      columns:
          - name: time_of_day
            description: "The date"
            tests:
                - unique
                - not_null