
version: 2

models:
    - name: dim_date
      description: "A starter dbt model"
      columns:
          - name: date_day
            description: "The date"
            tests:
                - unique
                - not_null
    - name: dim_time
      description: "A starter dbt model"
      columns:
          - name: time_of_day
            description: "The date"
            tests:
                - unique
                - not_null